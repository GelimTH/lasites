import type { MockupMeta } from '@/types/catalog';

const meta: MockupMeta = {
  label: 'Restaurante',
  description: 'Landing page gastronômica — Il Cortile',
  tag: 'gastronomia',
};

export default meta;
