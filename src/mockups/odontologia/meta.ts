import type { MockupMeta } from '@/types/catalog';

const meta: MockupMeta = {
  label: 'Odontologia',
  description: 'Landing page clínica odontológica — Sorrir',
  tag: 'saude',
};

export default meta;
